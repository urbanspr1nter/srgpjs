export interface SRGPGloalContext {
    out: HTMLCanvasElement;
    context: CanvasRenderingContext2D;
    width: number;
    height: number;
}

export interface Point {
    x: number;
    y: number;
}
